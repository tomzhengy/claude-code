interface:
  display_name: "Code Simplifier"
  short_description: "Simplify code without changing behavior"
  default_prompt: "Use $code-simplifier to simplify the recent changes without changing behavior."

policy:
  allow_implicit_invocation: false
