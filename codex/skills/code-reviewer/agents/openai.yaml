interface:
  display_name: "Code Reviewer"
  short_description: "Review code for bugs, risks, and gaps"
  default_prompt: "Use $code-reviewer to review the recent changes for bugs, regressions, and missing tests."

policy:
  allow_implicit_invocation: false
